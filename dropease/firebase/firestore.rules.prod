rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Production: require authentication by default. Adjust per-collection rules as needed.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
