import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Business Opportunities – DropEase",
  description: "Discover profitable products, suppliers, and market gaps to grow your dropshipping business.",
}

export default function BusinessOpportunitiesLayout({ children }: { children: React.ReactNode }) {
  return children
}