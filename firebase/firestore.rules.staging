rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Staging: Open rules for development and testing
    // WARNING: Do NOT use these rules in production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
